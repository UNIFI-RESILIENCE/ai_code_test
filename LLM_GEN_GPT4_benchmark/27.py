def flip_case(string: str) -> str:
    # Use the built-in str.swapcase() method to flip the case of each character
    # Lowercase characters become uppercase and vice versa
    return string.swapcase()