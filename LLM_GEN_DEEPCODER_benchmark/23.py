def strlen(string: str) -> int:
    """Return the length of the input string.
    
    Args:
        string: Input string whose length needs to be calculated.
        
    Returns:
        Integer representing the length of the string.
        
    Note:
        This leverages Python's built-in len() function for optimal performance.
    """
    return len(string)